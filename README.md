# DataAgent-dsh

Harness-agnostic DataAgent core with DeepSeek Harness integration.

> Bootstrap repository. Implementation is developed on a migration branch and only promoted to `main` after CI validation.
