# MCP PoC only. Production MUST inject authenticated identity and MUST NOT use static principal.
AGENT3_MCP_POC_MODE=0

# dsh local inference endpoint credential reference.
LOCAL_LLM_KEY=change-me

# Defense in depth: deployment should also disable telemetry plugin lines.
DSH_TELEMETRY_MODE=DISABLED
